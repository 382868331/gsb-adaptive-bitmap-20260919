# 完整首轮任务

请从零实现“自适应压缩位图与秩选择库”，用于检索索引 / 压缩数据结构中的本地可复用模块。

1. 从零实现 uint32 集合库，按高 16 位分桶，低 16 位为容器；基数<=4096 使用有序 uint16 数组，否则使用 1024 个 uint64 位图。更新和集合运算后必须按阈值选择规范表示。
2. 提供 Add/Remove/Contains、Union/Intersect/Difference、递增迭代、Rank(x)=小于等于x的元素数和从0开始的Select(k)；不存在的秩返回明确错误。计数须能表示整个 uint32 域的基数。
3. 实现容器配对的数组/位图集合算法，禁止每个操作先展开成所有整数或 map。Rank/Select 利用桶基数与字级 popcount，不按整个 2^32 域逐值扫描；统计实际容器字节以展示转换。
4. 提供确定性二进制保存/读取，规范包括版本、桶序、类型、基数和载荷；拒绝重复/乱序桶、重复数组值、基数不符、截断和非规范容器。解码资源受调用者总字节上限约束。
5. 用标准 map 集合作小范围参考验证操作序列与集合代数；仅核心库与演示，不做文件管理器或搜索网站。

验收重点：4096/4097 边界往返、0与2^32-1、空集、跨桶 Rank/Select、混合容器集合运算、坏序列化。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Go 1.26.5标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；go run ./cmd/demo须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为go test ./... -count=1 -timeout=60s。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
