# 完整首轮任务

写一个支持集合运算、秩查询和按序选择的压缩位图库。数据稀疏时用数组，变密后切换为位图；请从零实现容器及其切换逻辑。

实现uint32集合，按高16位分桶；低16位基数<=4096使用升序uint16数组，否则使用1024个uint64位图。更新及集合运算后按阈值转换，桶按高位排序。

提供Add/Remove/Contains、Union/Intersect/Difference、递增迭代、Rank(x)=元素<=x的数量、0起始Select(k)。总基数和秩用uint64；Select越界报错。集合运算返回新集合，不改写或共享可写容器；单元素增删可原地进行。

实现数组/位图不同组合的运算，不能先全部展开成整数map。Rank/Select利用桶基数和字级popcount，不扫描整个uint32域；不要求持久化树或并发。

提供确定性二进制保存/读取并说明格式；拒绝重复/乱序桶、重复数组值、错误基数、截断和非规范容器。在分配前按调用者载荷字节上限检查；载荷统计固定为数组元素数×2或位图8192字节，明确不是Go进程内存。

用map参考验证小样本集合代数与Rank/Select；用一个4097元素案例检查表示往返，不生成2^32规模数据。演示稀疏/稠密转换及跨桶查询。

测试里请覆盖这些情况：4096/4097往返、0与MaxUint32、空集、跨桶Rank/Select、运算输入不变、坏序列化、载荷统计。

使用 Go 1.26.5 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `go run ./cmd/demo` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `go test ./... -count=1 -timeout=60s` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
