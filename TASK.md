# 完整首轮任务

离线检索索引需要压缩保存文档ID，并执行集合运算与排名定位。请从零实现“自适应压缩位图与秩选择库”。

1. 实现uint32集合，按高16位分桶；低16位基数<=4096使用升序uint16数组，否则使用1024个uint64位图。更新及集合运算后按阈值转换，桶按高位排序。
2. 提供Add/Remove/Contains、Union/Intersect/Difference、递增迭代、Rank(x)=元素<=x的数量、0起始Select(k)。总基数和秩用uint64；Select越界报错。集合运算返回新集合，不改写或共享可写容器；单元素增删可原地进行。
3. 实现数组/位图不同组合的运算，不能先全部展开成整数map。Rank/Select利用桶基数和字级popcount，不扫描整个uint32域；不要求持久化树或并发。
4. 提供确定性二进制保存/读取并说明格式；拒绝重复/乱序桶、重复数组值、错误基数、截断和非规范容器。在分配前按调用者载荷字节上限检查；载荷统计固定为数组元素数×2或位图8192字节，明确不是Go进程内存。
5. 用map参考验证小样本集合代数与Rank/Select；用一个4097元素案例检查表示往返，不生成2^32规模数据。演示稀疏/稠密转换及跨桶查询。

验收重点：4096/4097往返、0与MaxUint32、空集、跨桶Rank/Select、运算输入不变、坏序列化、载荷统计。

工程约束：全新0-1核心库，仅用Go 1.26.5标准库，在Windows原生离线运行，无第三方依赖、外部服务、Docker或前后端。交付实现、聚焦上述边界的测试及简短README；演示命令`go run ./cmd/demo`用小案例在约8秒内打印正常结果和一种真实失败，测试命令`go test ./... -count=1 -timeout=60s`。固定种子随机验证只用少量小样本，避免长压测和额外平台功能，不限制正常纠错所需工具调用。
一次首轮完成，不要求用户追加“继续”。只操作当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区；不添加Actions/CI/部署/Dependabot、不远程推送，不硬编码演示或虚报验证。
